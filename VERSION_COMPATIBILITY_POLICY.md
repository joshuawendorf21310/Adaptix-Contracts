# Contracts Version Compatibility Policy

- semantic versioning is required
- breaking changes require major version bump
- all event contracts must include explicit version metadata
- downstream repos must declare supported versions
